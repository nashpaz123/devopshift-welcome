pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'IDforuserandpassword', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
                    echo "Deploying with username ${env.USERNAME}"
                    // Use the credentials in your deployment steps
                    sh 'echo $USERNAME is my user name'
                    sh 'echo $PASSWORD is my pass'
                }
            }
        }
    }
}
